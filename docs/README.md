# Documentation

One folder per AppSheet application, containing what it does, its data
model, and any automation that supports it.

Suggested layout per app:

```
docs/
  <app-name>/
    README.md      # purpose, data sources, key views/automations
```
