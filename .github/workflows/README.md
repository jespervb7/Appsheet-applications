# Workflows

GitHub Actions that run the automation in `src/` — e.g. on a schedule,
or in response to a push. Add workflow `.yml` files here as automations
are built; reference secrets via repository Actions secrets, never
hard-coded values.
